/* eslint-disable no-unused-vars */

import { firebaseConfig } from "@/store/config/firebase"
export const user = {
	state() {
		return {
			user: null
		}
	},

	mutations: {},

	getters: {},

	actions: {},

	namespaced: true
}